//
// sample.cpp
// Created by wasifshahzad on 8/23/25 at 13:53:17.
//

#include <bits/stdc++.h>
using namespace std;
#define int long long
#pragma GCC optimize("O3,unroll-loops")
#pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt")

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            int x; cin >> x;
        }
    }
    int a;
    cin >> a;
    for (int i = 0; i < a; i++) {
        int x; cin >> x;
    }
    int q;
    cin >> q;
    while (q--) {
        int i, j;
        cin >> i >> j;
    }
    cout << "-1 -1\n";
    cout << "1 6\n";
    cout << "-1 -1\n";
    cout << "-1 -1\n";
    cout << "1 12\n";
}